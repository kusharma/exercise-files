name: first
on: push
jobs:
    job1:
        name: first job
        runs-on: ubuntu-latest
        steps: 
            - name: first step
              run: echo "first step"
              uses: actions/checkout@v2
            - name: second step
              run: echo "second step"
    job2:
        name: second job
        runs-on: macos-latest
        steps: 
            - name: first step
              run: echo "first step"
              uses: actions/checkout@v2
            - name: second step
              run: echo "second step"
